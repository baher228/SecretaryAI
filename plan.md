1. Initialize the Python backend skeleton and package tooling.
2. Define architecture contracts (`domain/models.py`) and route skeletons.
3. Keep `SecretaryService` as the orchestration boundary with TODO methods only.
4. Expose `GET /architecture` for team onboarding during the hackathon.
5. Defer integrations (telephony/AI/calendar/notifications/storage) to next iteration.
