services:
  secretary-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: secretary-ai
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./.telegram:/app/.telegram
    restart: unless-stopped
